# Arrays（Niagara 内置模块）

# Arrays（Niagara 内置模块）

- 条目数：`9`
- 说明：模块名和参数名保持 UE 原始英文；中文内容用于说明如何查阅和使用。
- 参数默认值和可写格式以当前项目导出的 Niagara folder JSON 为准。

## `FillBoolArray`

- 参数名：`StartIndex`、`Array Size`、`Bool Array`、`bool1`、`bool10`、`bool2`、`bool3`、`bool4`、`bool5`、`bool6`、`bool7`、`bool8`、`bool9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillColorArray`

- 参数名：`StartIndex`、`Array Size`、`Color Array`、`Color1`、`Color10`、`Color2`、`Color3`、`Color4`、`Color5`、`Color6`、`Color7`、`Color8`、`Color9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillFloatArray`

- 参数名：`StartIndex`、`Array Size`、`Float Array`、`float1`、`float10`、`float2`、`float3`、`float4`、`float5`、`float6`、`float7`、`float8`、`float9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillIntArray`

- 参数名：`StartIndex`、`Array Size`、`Int32 Array`、`Integer1`、`Integer10`、`Integer2`、`Integer3`、`Integer4`、`Integer5`、`Integer6`、`Integer7`、`Integer8`、`Integer9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillPositionArray`

- 参数名：`StartIndex`、`Array Size`、`Position Array`、`Position1`、`Position10`、`Position2`、`Position3`、`Position4`、`Position5`、`Position6`、`Position7`、`Position8`、`Position9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillQuaternionArray`

- 参数名：`StartIndex`、`Array Size`、`Quat1`、`Quat10`、`Quat2`、`Quat3`、`Quat4`、`Quat5`、`Quat6`、`Quat7`、`Quat8`、`Quat9`、`Quaternion Array`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillVector2DArray`

- 参数名：`StartIndex`、`Array Size`、`Vector 2D Array`、`Vector1`、`Vector10`、`Vector2`、`Vector3`、`Vector4`、`Vector5`、`Vector6`、`Vector7`、`Vector8`、`Vector9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillVector4Array`

- 参数名：`StartIndex`、`Array Size`、`Vector 4 Array`、`Vector1`、`Vector10`、`Vector2`、`Vector3`、`Vector4`、`Vector5`、`Vector6`、`Vector7`、`Vector8`、`Vector9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `FillVectorArray`

- 参数名：`StartIndex`、`Array Size`、`Vector Array`、`Vector1`、`Vector10`、`Vector2`、`Vector3`、`Vector4`、`Vector5`、`Vector6`、`Vector7`、`Vector8`、`Vector9`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

