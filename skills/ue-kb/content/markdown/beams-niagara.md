# Beams（Niagara 内置模块）

# Beams（Niagara 内置模块）

- 条目数：`6`
- 说明：模块名和参数名保持 UE 原始英文；中文内容用于说明如何查阅和使用。
- 参数默认值和可写格式以当前项目导出的 Niagara folder JSON 为准。

## `BeamEmitterSetup`

- 参数名：`BeamEnd`、`BeamEndTangent`、`BeamStart`、`BeamStartEndLength`、`BeamStartTangent`、`Absolute End Tangent`、`Absolute Start Tangent`、`Beam End`、`Beam End Tangent`、`Beam Start`、`Beam Start Tangent`、`Use Beam Tangents`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `BeamWidth`

- 参数名：`Beam Twist Amount`、`Beam Width`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `BeamWidthScale`

- 参数名：未从资产元数据中提取到显式参数。
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `ScaleBeamWidth`

- 参数名：`Beam Width Scale`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SpawnBeam`

- 参数名：`BeamDirection`、`BeamVector`、`NormalizedExecIndex`、`StartEndVecLength`、`Spline Position Error Threshold`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `UpdateBeam`

- 参数名：`BeamDirection`、`BeamVector`、`StartEndVecLength`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

