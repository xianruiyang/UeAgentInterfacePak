# Emitter（Niagara 内置模块）

# Emitter（Niagara 内置模块）

- 条目数：`6`
- 说明：模块名和参数名保持 UE 原始英文；中文内容用于说明如何查阅和使用。
- 参数默认值和可写格式以当前项目导出的 Niagara folder JSON 为准。

## `EmitterLifeCycle`

- 参数名：`AutoComplete`、`CompleteOnInactive`、`DelayFirstLoopOnly`、`DurationRecalcEachLoop`、`MaxLoopCount`、`NextLoopDelay`、`NextLoopDuration`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `EmitterState`

- 参数名：`LoopCountIncreased`、`LoopDuration`、`NoActiveEmitterParticles`、`PreviousEmitterState`、`ScalabilityKillOnComplete`、`ScalabilityKillOnCompleteMin`、`bApplySpawnCountScaleByDistance`、`bUseMaxDistance`、`bUseMinDistance`、`Delay First Loop Only`、`Loop Count`、`Loop Delay`、`Loop Duration`、`Max Distance Response`、`MaxDistance`、`Min Distance Response`、`MinDistance`、`Recalculate Duration Each Loop`、`Reset Age On Awaken`、`Spawn Count Scale`、`Spawn Count Scale By Distance`、`Visibility Cull Delay`、`Visibility Cull Response`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SpawnBurst_Instantaneous`

- 参数名：`CountNotEqualZero`、`SpawnCount`、`SpawningIsValid`、`SpawnStartDT`、`Age`、`Loop Count Limit`、`Spawn Count`、`Spawn Group`、`Spawn Probability`、`Spawn Time`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SpawnPerFrame`

- 参数名：`Spawn`、`Spawn Count`、`Spawn Group`、`Spawn Probability`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SpawnPerUnit`

- 参数名：`InterpStartDt`、`IntervalDt`、`MovementThresholdVectorLength`、`SpawnCountInt`、`SpawnSpacing`、`VelocityVectorLength`、`Delta Time`、`Max Movement Threshold`、`Movement Tolerance`、`Spawn Group`、`Spawn Per Unit`、`Spawn Probability`、`Spawn Spacing`、`Use Max Movement Threshold`、`Use Movement Tolerance`、`Velocity Vector`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SpawnRate`

- 参数名：`InterpStartDT`、`IntervalDT`、`SpawnCount`、`Spawn Probability`、`SpawnGroup`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

