# CascadeConversion（Niagara 内置模块）

# CascadeConversion（Niagara 内置模块）

- 条目数：`4`
- 说明：模块名和参数名保持 UE 原始英文；中文内容用于说明如何查阅和使用。
- 参数默认值和可写格式以当前项目导出的 Niagara folder JSON 为准。

## `CascadeConversion_InitialMeshRotation`

- 参数名：`Inherit Parent Rotation`、`Rotation`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `CascadeConversion_LightProperties`

- 参数名：`LightBrightnessScale`、`LightColorScale`、`LightRadiusScale`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `CascadeConversion_Orbit`

- 参数名：`Offset`、`Rotation`、`Rotation Rate`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `CascadeConversion_SolveOrbit`

- 参数名：未从资产元数据中提取到显式参数。
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

