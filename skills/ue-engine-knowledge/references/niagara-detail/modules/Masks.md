# Masks（Niagara 内置模块）

- 条目数：`3`
- 说明：模块名和参数名保持 UE 原始英文；中文内容用于说明如何查阅和使用。
- 参数默认值和可写格式以当前项目导出的 Niagara folder JSON 为准。

## `ConeMask`

- 参数名：`Cone Angle`、`Cone Axis`、`Falloff Angle`、`Length`、`Length Falloff`、`Particle Position`、`PivotPoint`、`Set Length`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `ConeMask`

- 参数名：`Cone Apex`、`Cone Axis`、`Coordinate Space`、`Inner Cone Angle`、`Outer Cone Angle`、`Radial Falloff`、`Radius`、`Sample Position`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `RegionMask`

- 参数名：`Center`、`Offset`、`OwnerRotation`、`Position`、`ReverseTransformOrder`、`Rotation`、`Scale`、`Debug Draw`、`Inner Color`、`Inner Float`、`Inner Vector`、`Inner Vector2D`、`Outer Color`、`Outer Float`、`Outer Vector`、`Outer Vector2D`、`Region Dimensions`、`Region Falloff`、`Region Origin`、`Region Radius`、`Region Slab Width`、`Transform Order`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。
