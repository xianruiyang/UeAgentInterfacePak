# Spawn（Niagara 内置模块）

- 条目数：`45`
- 说明：模块名和参数名保持 UE 原始英文；中文内容用于说明如何查阅和使用。
- 参数默认值和可写格式以当前项目导出的 Niagara folder JSON 为准。

## `InitialDecalOrientation`

- 参数名：`OrientationQuat`、`RotationQuat`、`Decal Axis to Orient`、`Orientation Matrix`、`Orientation Quaternion`、`Orientation Vector`、`Random Seed`、`Rotation`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `InitializeParticle`

- 参数名：`Color`、`Lifetime`、`Mass`、`Material Random`、`Mesh Scale`、`Position`、`Sprite Rotation`、`Sprite Size`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `InitializeRibbon`

- 参数名：`InitRIbbonDistance`、`Alpha Scale Range`、`Color`、`Color Maximum`、`Color Minimum`、`Hue Shift Range`、`Lifetime`、`Lifetime Max`、`Lifetime Min`、`Mass`、`Mass Max`、`Mass Min`、`Material Random`、`Maximum`、`Minimum`、`Position`、`Position Offset`、`Position Offset Coordinate Space`、`Ribbon Facing Vector`、`Ribbon Twist`、`Ribbon Width`、`Saturation Range`、`Value Range`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `InitializeParticle`

- 参数名：`InitRIbbonDistance`、`Alpha Scale Range`、`Color`、`Color Maximum`、`Color Minimum`、`Hue Shift Range`、`Lifetime`、`Lifetime Max`、`Lifetime Min`、`Mass`、`Mass Max`、`Mass Min`、`Material Random`、`Maximum`、`Mesh Renderer Info`、`Mesh Renderer Mesh Index`、`Mesh Renderer Random Seed`、`Mesh Scale`、`Mesh Scale Max`、`Mesh Scale Min`、`Mesh Uniform Scale`、`Mesh Uniform Scale Max`、`Mesh Uniform Scale Min`、`Minimum`、`Position`、`Position Offset`、`Position Offset Coordinate Space`、`Ribbon Facing Vector`、`Ribbon Twist`、`Ribbon Width`、以及另外 `1` 个参数
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `BoxLocation`

- 参数名：`BoxNormal`、`BoxVector`、`Debug Draw`、`PretransformedBoxNormal`、`Box Offset`、`Box Origin`、`Box Size`、`Coordinate Space`、`Fixed Random Seed`、`Normalized Box Rotation Angle`、`Override Randomness`、`Override Seed`、`Random Seed`、`Randomness Mode`、`Rotate Box On Axis`、`Spawn Group Mask`、`Surface Expansion Mode`、`Surface Only Band Thickness`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `ConeLocation`

- 参数名：`ConeVector`、`Cone Angle`、`Cone Axis`、`Cone Length`、`Cone Origin`、`Coordinate Space`、`Enable Wedge`、`Fixed Random Seed`、`Flatten Endcaps`、`Offset`、`Override Randomness`、`Override Seed`、`Point Distribution`、`Random Seed`、`Randomness Mode`、`Spawn Group Mask`、`Wedge Horizontal Angle`、`Wedge Vertical Angle`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `CurlNoiseLocation`

- 参数名：`CurlNoise`、`Noise Frequency`、`Noise Offset`、`Noise Strength`、`Per Particle Noise Sample Offset`、`Use Per Particle Noise`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `CylinderLocation`

- 参数名：`CylinderHeight`、`CylinderNormal`、`CylinderRadius`、`CylinderSurfaceArea`、`CylinderVector`、`EndcapLateralRatio`、`EndcapRatio`、`Endcaps`、`EndcapSurfaceArea`、`IsEndcap`、`IsEndcapTop`、`LateralEndcapRatio`、`LateralSurfaceAreaLarger`、`LatheProfileDerivative`、`NormalizedCylinderVector`、`OffsetVector`、`PretransformedCylinderNormal`、`RandomAngle`、`RandomDirection`、`RandomFloat01`、`RandomRadius`、`RandomRatio`、`RandomVector01`、`RandomZ`、`SurfaceOnly`、`SurfaceOnlyZAxis`、`UseEndcapsSurfaceOnly`、`XAxis`、`YAxis`、`37`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `CylinderLocation_System`

- 参数名：`CylinderHeight`、`CylinderRadius`、`CylinderVector`、`EndcapRatio`、`Endcaps`、`RandomAngle`、`RandomDirection`、`RandomRadius`、`RandomRatio`、`RandomZ`、`SurfaceOnly`、`SurfaceOnlyZAxis`、`UseEndcapsSurfaceOnly`、`XAxis`、`YAxis`、`Cylinder Height`、`Cylinder Origin`、`Cylinder Radius`、`Enable Lathe Profile`、`Hemisphere X`、`Hemisphere Y`、`Lathe Profile`、`Non Uniform Scale`、`Offset`、`Orientation Axis`、`Override Randomness`、`Override Seed`、`Random Seed`、`Randomness Mode`、`Surface Only`、以及另外 `1` 个参数
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `ENiagaraGridPlacementType`

- 参数名：未从资产元数据中提取到显式参数。
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `GenerateGridRibbonIDs`

- 参数名：`GridLocationGrid X Count`、`GridLocationGrid Y Count`、`GridLocationGrid Z Count`、`GridLocationInitial Grid Spacing`、`Grid Line Frequency`、`Grid X Count`、`Grid Y Count`、`Grid Z Count`、`Initial Grid Spacing`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `GridLocation`

- 参数名：`Grid X Count`、`Grid Y Count`、`Grid Z Count`、`Initial Grid Spacing`、`Local Space Position`、`XYZCountAsVector`、`Coordinate Space`、`Dimensions Definition`、`Exec Index`、`Grid Origin`、`Jitter Inside Grid Cells`、`Offset`、`Randomize Placement Within Cell`、`XYZ Dimensions`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `RotateAroundPoint`

- 参数名：`Cosine`、`Sine`、`XAxis`、`YAxis`、`Delta Time`、`Radius`、`Rotation Axis`、`Rotation Center`、`Rotation Phase`、`Rotation Rate`、`Set Particle Position`、`X Axis`、`Y Axis`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SkeletalMeshLocation`

- 参数名：未从资产元数据中提取到显式参数。
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SkeletalMeshSkeletonLocation`

- 参数名：`MeshPosition`、`MeshRotation`、`MeshVelocity`、`Inherit Velocity`、`Inherit Velocity From Mesh`、`Sampled Position Attribute`、`Sampled Position Offset`、`Sampled Rotation Attribute`、`Sampled Velocity Attribute`、`Set Mesh Orientation to Bone Orientation`、`Use Sampled Bone as Ribbon ID`、`Use Sampled Bone Index as Ribbon ID`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SkeletalMeshSurfaceLocation`

- 参数名：`MeshNormal`、`MeshPosition`、`MeshVelocity`、`Add Velocity Along Mesh Normals`、`Add Velocity from Mesh Normals`、`Enable Offset Spawn Position`、`Inherit Velocity`、`Inherit Velocity From Mesh Amount`、`Offset Spawn Position Along Mesh Normal`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SocketLocation`

- 参数名：`Position`、`Rotation`、`SampledPosition`、`SampledRotation`、`SampledScale`、`SampledVelocity`、`Velocity`、`WorldToComponentRotation`、`Inherit Velocity (Scale)`、`Inherited Velocity Speed Limit`、`Random Seed`、`Sampled Position Offset`、`Scale`、`Socket Index`、`Socket Reader`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `Spawn_MS_VertexAnimationTools_MorphTarget_Particles`

- 参数名：`Age`、`Morph Target Texture`、`Number of Particles Per Vertex`、`Spawn Count`、`Spawn Group`、`Spawn Probability`、`Spawn Time`、`SpawnBurst_Instantaneous.Spawn Count`、`SpawnBurst_Instantaneous.Spawn Time`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SpawnParticlesInGrid`

- 参数名：`Age`、`Spawn Group`、`Spawn Time`、`X Count`、`Y Count`、`Z Count`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SphereLocation`

- 参数名：`SphereVector`、`Coordinate Space`、`Fixed Random Seed`、`Hemisphere X`、`Hemisphere Y`、`Hemisphere Z`、`Non Uniform Scale`、`Normalize Thickness`、`Offset`、`Override Randomness`、`Override Seed`、`Radius Position`、`Random Seed`、`Randomness Mode`、`Spawn Group Mask`、`Sphere Origin`、`Sphere Radius`、`Surface Expansion Mode`、`Surface Only Band Thickness`、`U Position`、`V Position`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SphereLocation_System`

- 参数名：`SphereVector`、`Fixed Random Seed`、`Hemisphere X`、`Hemisphere Y`、`Hemisphere Z`、`Non Uniform Scale`、`Normalize Thickness`、`Offset`、`Override Randomness`、`Override Seed`、`Radius Position`、`Random Seed`、`Randomness Mode`、`Sphere Origin`、`Sphere Radius`、`Surface Expansion Mode`、`Surface Only Band Thickness`、`U Position`、`V Position`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `StaticMeshLocation`

- 参数名：`RandomInfo`、`SampledBitangent`、`SampledMeshTriCoord`、`SampledMeshUV`、`SampledMeshVertexID`、`SampledNormal`、`SampledPosition`、`SampledRotation`、`SampledScale`、`SampledSocketIndex`、`SampledTangent`、`SampledVelocity`、`SampledVertexColor`、`SpawnGroupMaskBool`、`SpawnGroupMaskValue`、`Enable Axis Constraint`、`Inherit Velocity (Scale)`、`Inherited Velocity Speed Limit`、`Initialize Particle Position To Sampled Position`、`Material Section Mask`、`Normal Offset`、`Offset Position Along Sampled Normal`、`Random Seed`、`RGBA Color Application Mask`、`Sampled Mesh Normal`、`Sampled Mesh Position`、`Sampled Position Offset`、`Sampled UV Index`、`Socket Index`、`Soft Source Movement Scale`、以及另外 `1` 个参数
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SystemLocation`

- 参数名：`Offset`、`Offset Coordinate Space`、`Spawn Group Mask`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `TorusLocation`

- 参数名：`Debug Draw Data Interface`、`RandomRadius`、`RandomU`、`RandomV`、`TorusBitangent`、`TorusNormal`、`TorusTangent`、`TorusVector`、`Coordinate Space`、`EnableScale`、`Fixed Random Seed`、`Handle Radius`、`Knot Helix Loops`、`Knot Ring Loops`、`Large Radius`、`Non Uniform Scale`、`Normalized Angle Around Torus Axis`、`Offset`、`Override Randomness`、`Override Seed`、`Radius Position`、`Random Seed`、`Randomness Mode`、`Spawn Group Mask`、`Surface Position`、`Torus Axis`、`Torus Knot Tube Radius`、`Torus Origin`、`U Distribution`、`U Position`、以及另外 `1` 个参数
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `GridLocation`

- 参数名：`Grid X Count`、`Grid Y Count`、`Grid Z Count`、`Initial Grid Spacing`、`Local Space Position`、`Coordinate Space`、`Custom Transformation Matrix`、`Exec Index`、`Grid Origin`、`Jitter Inside Grid Cells`、`Offset`、`Override Random Seed`、`Placement Randomness Mode`、`Random Placement Seed`、`Randomize Overriden Seed`、`Randomize Placement Within Cell`、`XYZ Dimensions`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `RotateAroundPoint`

- 参数名：`BasisVectorsAreUpsideDown`、`Cone Apex`、`Cone Axis`、`Cosine`、`CurrentBankingQuat`、`Incoming Rotation Axis`、`Line Segment End Position`、`Line Segment Start Position`、`Local Line Segment`、`Model Y Axis`、`Nearest Surface Normal`、`Particle orientation Matrix`、`Position`、`PreviousIncrementingCounterValue`、`PreviousRotatedPosition`、`PreviousSafeRotationAxis`、`PreviousTotalRotationAmount`、`Rotation Axis`、`SimpleQuat`、`Sine`、`SmoothZVectorValue`、`Start Point To Position Vector`、`ThisFramesAngleOffsetOffset`、`TransformedUpVector`、`VelocityMagnitude`、`XAxis`、`YAxis`、`Delta Time`、`Influence`、`Point To Rotate`、以及另外 `1` 个参数
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `ShapeLocation`

- 参数名：`BoxNormal`、`BoxTangent`、`BoxVector`、`ConeNormal`、`ConeTangent`、`ConeVector`、`CylinderNormal`、`CylinderSurfaceArea`、`CylinderTangent`、`CylinderVector`、`EndcapLateralRatio`、`EndcapSurfaceArea`、`InverseScaleFactor`、`IsEndcap`、`IsEndcapTop`、`LateralEndcapRatio`、`LateralSurfaceAreaLarger`、`LatheProfileDerivative`、`NormalizedCylinderVector`、`OffsetVector`、`Random0to1Float_01`、`Random0to2Float_01`、`RandomBool`、`RandomRadius`、`RandomU`、`RandomV`、`RandomVector01`、`RingBitangent`、`RingNormal`、`RingTangent`、以及另外 `1` 个参数
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SkeletalMeshLocation`

- 参数名：`RandomInfo`、`SampledBitangent`、`SampledBoneIndex`、`SampledMeshTriCoord`、`SampledMeshUV`、`SampledMeshVertexID`、`SampledNormal`、`SampledPosition`、`SampledRotation`、`SampledScale`、`SampledTangent`、`SampledVelocity`、`SampledVertexColor`、`SkeletalMesh`、`SpawnGroupMaskBool`、`SpawnGroupMaskValue`、`Apply To Particle Position`、`Fixed Random Seed`、`Inherit Velocity (Scale)`、`Inherited Velocity Speed Limit`、`Initialize Particle Position To Sampled Position`、`Offset Position Along Sampled Normal`、`Override Randomness`、`Override Seed`、`Random Seed`、`Randomness Mode`、`Sampled Position Offset`、`Sampled UV Index`、`Skeletal Mesh`、`6`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SphereLocation`

- 参数名：`Debug Draw Data Interface`、`SphereVector`、`Hemisphere X`、`Hemisphere Y`、`Hemisphere Z`、`Non Uniform Scale`、`Normalize Thickness`、`Offset`、`Offset Coordinate Space`、`Radius Position`、`Random Seed`、`Spawn Group Mask`、`Sphere Coordinate Space`、`Sphere Orientation Axis`、`Sphere Origin`、`Sphere Radius`、`Surface Expansion Mode`、`Surface Only Band Thickness`、`U Position`、`Uniform Distribution`、`Uniform Endpoint Offset`、`Uniform Spiral Amount`、`V Position`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `Initialize_MeshReproductionSprite`

- 参数名：`Mesh Binormal`、`Mesh Normal`、`Mesh Tangent`、`Mesh UV`、`Scale Modulation Value`、`Velocity`、`Kill Particles Based on the Min Particle Size Restriction`、`Kill Particles Below X Width`、`Max Particle Size`、`Mesh`、`Overwrite Intrinsic Variables`、`Particle Scale`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SampleSkeletalMeshSkeleton`

- 参数名：`PositionBool`、`RotationBool`、`SampledBoneIndex`、`SpecificBoneCount`、`SpecificSocketCount`、`VelocityBool`、`Interpolate Bone Sampling`、`Mesh`、`Sampled Position Coordinate Space`、`Sampled Rotation Coordinate Space`、`Sampled Velocity Coordinate Space`、`Spawn Mode`、`Specific Bone Index`、`Specific Socket Index`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SampleSkeletalMeshSurface`

- 参数名：`Interpolated`、`Skeletal Mesh`、`UV Index`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SampleStaticMesh`

- 参数名：`RandomInfo`、`Random Seed`、`StaticMesh`、`UV`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SampleStaticMesh_PositionOnly`

- 参数名：`StaticMesh`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SampleSkeletalMeshSurface`

- 参数名：`MeshTriCoord`、`MeshVertexID`、`PositionBool`、`SurfaceBool`、`VelocityBool`、`Direct Tri Coordinate`、`Direct Vertex ID`、`Fixed Random Seed`、`Interpolate Surface Sampling`、`Mesh`、`Override Seed`、`Random Seed`、`Randomness Mode`、`Sampled Position Coordinate Space`、`Sampled Surface Coordinate Space`、`Sampled Velocity Coordinate Space`、`UV Index`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `InitialMeshRotation`

- 参数名：`Orientation Axis`、`Pitch`、`Roll`、`Use Orientation Axis`、`Yaw`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `InitialMeshOrientation`

- 参数名：`Temp`、`Orientation Axis`、`Orientation Coordinate Space`、`Orientation Vector`、`Rotation`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `InitialMeshOrientation`

- 参数名：`OrientationQuat`、`RotationQuat`、`Temp`、`Mesh Axis To Orient`、`Orientation Axis`、`Orientation Coordinate Space`、`Orientation Matrix`、`Orientation Quaternion`、`Orientation Vector`、`Random Seed`、`Rotation`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `AddVelocity`

- 参数名：`LocalTransformBitangent`、`LocalTransformNormal`、`LocalTransformTangent`、`LocalTransformVector`、`TransformedVector`、`Cone Angle`、`Cone Axis`、`Constrain To Radius`、`Coordinate Space`、`Default Position`、`Distribution Along Cone Axis`、`Inner Cone Angle`、`Invert Rotation Quaternion`、`Invert Velocity Falloff`、`Origin Offset`、`Origin Offset Coordinate Space`、`Pitch`、`Radius Falloff Exponent`、`Random Seed`、`Roll`、`Rotation Angle`、`Rotation Axis`、`Rotation Coordinate Space`、`Rotation Matrix`、`Rotation Quaternion`、`Scale Added Velocity`、`Speed Falloff From Cone Axis`、`Use Falloff Distance`、`8`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `AddVelocityFromPoint`

- 参数名：`Default Position`、`Invert Velocity Falloff`、`Origin Offset`、`Origin Offset Coordinate Space`、`Use Falloff Distance`、`Velocity Falloff Distance`、`Velocity Origin`、`Velocity Strength`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `AddVelocityInCone`

- 参数名：`ConeVector`、`DotBetweenConeVectorAndAxis`、`Cone Angle`、`Cone Axis`、`Cone Axis Coordinate Space`、`Use Velocity Falloff On Cone Axis`、`Velocity Distribution Along Cone Axis`、`Velocity Falloff Away From Cone Axis`、`Velocity Strength`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `LinearForce`

- 参数名：`Force`、`Coordinate Space`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `PointForce`

- 参数名：`DebugDraw`、`ForceFromPoint`、`Fallback Vector`、`Force Falloff Distance`、`Force Origin`、`Force Strength`、`Invert Falloff`、`Linear Force Vector`、`Particle Position`、`Use Falloff`、`UseLinearForce`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `ScaleVelocity`

- 参数名：`Coordinate Space`、`Scale Added Velocity`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `StaticMeshVelocity`

- 参数名：`Add Velocity Along Mesh Normals`、`Enable Inherit Velocity`、`Enable Mesh Normal Sampling`、`Inherit Velocity From Mesh Amount`、`Sampled Mesh Normal`、`Sampled Mesh Velocity`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。
