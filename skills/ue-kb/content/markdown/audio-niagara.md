# Audio（Niagara 内置模块）

# Audio（Niagara 内置模块）

- 条目数：`7`
- 说明：模块名和参数名保持 UE 原始英文；中文内容用于说明如何查阅和使用。
- 参数默认值和可写格式以当前项目导出的 Niagara folder JSON 为准。

## `EInitialAudioParamType`

- 参数名：未从资产元数据中提取到显式参数。
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `InitializeAudioPlayer`

- 参数名：`Audio Player`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `PlayAudio`

- 参数名：`Audio Playback Enabled`、`Audio Playback Position`、`Audio Player`、`Audio Position Coordinate Space`、`Audio Rotation`、`Pitch`、`Play Audio Probability`、`Play Audio Trigger`、`Rotation`、`Start Time`、`Volume`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `PlayLoopingAudio`

- 参数名：`PlaybackPosition`、`ShouldPlayAudio`、`Audio Playback Position`、`Audio Player`、`Audio Position Coordinate Space`、`Fade In Time`、`Fade Out Time`、`Pitch`、`Play Audio`、`Rotation`、`Start Time`、`Volume`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `PlayPersistentAudio`

- 参数名：`PlaybackPosition`、`ShouldPlayAudio`、`Audio Playback Position`、`Audio Player`、`Audio Position Coordinate Space`、`Camera Query`、`Fade In Time`、`Fade Out Time`、`Number Of Nearest Particles To Play Audio`、`Pitch`、`Play Audio`、`Rotation`、`Start Time`、`Volume`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `SetInitialAudioParameter`

- 参数名：`Audio Player`、`BoolValue`、`FloatValue`、`IntValue`、`Parameter Name Index`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

## `UpdatePersistentAudio`

- 参数名：`Audio Player`、`Bool Value`、`Float Value`、`Int Value`、`Is Paused`、`Parameter Name Index`、`Pitch`、`Position`、`Position Coordinate Space`、`Rotation`、`Volume`
- 使用提示：先通过 Niagara folder JSON 导出真实模块，再修改 module input；不要凭记忆写入参数。

