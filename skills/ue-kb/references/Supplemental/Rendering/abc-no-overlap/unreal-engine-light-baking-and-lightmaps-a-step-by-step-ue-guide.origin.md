# 光烘焙和光照贴图：UE 分步指南

## 来源与状态

- 原始 URL：https://dev.epicgames.com/community/learning/tutorials/KPOx/unreal-engine-light-baking-and-lightmaps-a-step-by-step-ue-guide
- 原始文件：unreal-engine-light-baking-and-lightmaps-a-step-by-step-ue-guide.origin.md
- 分段：第 1/3 段

- 原始 URL：https://dev.epicgames.com/community/learning/tutorials/KPOx/unreal-engine-light-baking-and-lightmaps-a-step-by-step-ue-guide

## 运行时分类

- 类型：图文教程
- 判断：运行时未识别到核心视频信号，可整理正文约 14069 字符。

## 摘要

本文提供了在虚幻引擎中进行光照烘焙的详细指南，重点介绍使用光照矩阵为静态对象创建高质量的烘焙光照...

## 中文整理

### 光烘焙和光照贴图：UE 分步指南

本文提供了虚幻引擎中光烘焙基础知识的全面指南，重点是使用光照贴图为静态对象创建高质量的烘焙光照。我们将探讨基本主题，例如设置光照贴图、配置光照、优化反射以及有效管理关卡层次结构。通过掌握这些技术，您可以增强项目的性能和视觉真实感，实现逼真的外观，同时优化场景效率。

### 光照贴图

本节将描述在 UE 中使用光照贴图的基本和高级原理。

光照贴图或照明贴图对于将光和阴影烘焙到网格的纹理中是必需的。

光照贴图仅适用于静态对象（静态网格物体）。

光源本身也必须是静态的或静止的（稍后会详细介绍）。

可移动光源不会影响光照烘焙，因为所有参数都是实时计算的。

光照贴图的主要要求如下： - 不应有重叠（展开时的交集）。

不应有重叠（展开时的交叉点）。

- 它不应超出第一个 UDIM。

它不应超出第一个 UDIM。

- 光照贴图的分辨率必须足以覆盖必要的纹素，以便光线烘焙质量。

光照贴图的分辨率必须足以覆盖必要的纹素，以便光线烘焙质量。

下面是 UE 自动构建的光照贴图的典型视图： 在大多数情况下，光照贴图是使用虚幻引擎提供的工具计算的。

默认情况下，导入网格时，UE 会计算对象的光照贴图。

然而，这种情况可能并不总是发生，因为由于各种原因，计算光照贴图的选项可能会被关闭（默认禁用或未启用）。

如果网格体带有预先存在的光照贴图，这是合理的。

显示所选网格的可用 UV 通道：为所选网格启用自动光照贴图生成：通常，光照贴图设置在第一个 UV 通道上。

然而，这不是强制性的。

该引擎允许选择用于光照贴图的 UV 通道。

重要的是要了解光照贴图只能从通道 1 到 4 进行采样（引擎将无法识别超出该通道的光照贴图）。

因此，光照贴图必须驻留在四个 UV 通道之一中： 选择将用于构建光照贴图的 UV 通道：如上所述，UE 可以自动计算光照贴图。

它是基于网格的 UV 通道之一形成的。

为了生成光照贴图，我们有多种选择。

如上图所示： - 我们可以看到引擎有 2 个 UV 通道。

事实上，这个网格体带有一个 UV 通道，第二个通道是由 UE 自动创建的。

我们注意到，在零通道中，UV 布局是用于平铺的。

有很多重叠之处，并且它超出了 UDIM 的范围。

此类参数不适合创建光照贴图。

同时，我们看到在第一个通道中，光照贴图是根据要求构建的，没有重叠，也没有超出 UDIM。

我们可以看到引擎有 2 个 UV 通道。

事实上，这个网格体带有一个 UV 通道，第二个通道是由 UE 自动创建的。

我们注意到，在零通道中，UV 布局是用于平铺的。

有很多重叠之处，并且它超出了 UDIM 的范围。

此类参数不适合创建光照贴图。

同时，我们看到在第一个通道中，光照贴图是根据要求构建的，没有重叠，也没有超出 UDIM。

- 可以使用选项来生成光照贴图。

首先，可以选择启用自动光照贴图生成。

源光照贴图索引 - 指示引擎使用哪个 UV 通道来采用布局来生成光照贴图。

在本例中，它是零通道。

目标光照贴图索引 - 指示将在其中生成光照贴图布局的 UV 通道。

在本例中，它是第一个通道。

最小光照贴图分辨率 - 创建最小纹理像素大小所需的非常重要的参数。

引擎中的最大值为 2048。

本质上，该值越高，光照贴图的纹素就越高。

可以使用选项来生成光照贴图。

首先，可以选择启用自动光照贴图生成。

- 源光照贴图索引 - 指示引擎使用哪个 UV 通道来采用布局来生成光照贴图。

在本例中，它是零通道。

源光照贴图索引 - 指示引擎使用哪个 UV 通道来采用布局来生成光照贴图。

在本例中，它是零通道。

- 目标光照贴图索引 - 指示将在其中生成光照贴图布局的 UV 通道。

在本例中，它是第一个通道。

目标光照贴图索引 - 指示将在其中生成光照贴图布局的 UV 通道。

在本例中，它是第一个通道。

- 最小光照贴图分辨率 - 创建最小纹素大小所需的非常重要的参数。

引擎中的最大值为 2048。

本质上，该值越高，光照贴图的纹素就越高。

最小光照贴图分辨率 - 创建最小纹理像素大小所需的非常重要的参数。

引擎中的最大值为 2048。

本质上，该值越高，光照贴图的纹素就越高。

- 例如，我们可能会遇到这样的情况：生成光照贴图后，它没有出现在 UDIM 中： 例如，我们可能会遇到这样的情况：生成光照贴图后，它没有出现在 UDIM 中： 这意味着我们用作创建基础的 UV 布局过于密集。

引擎无法展开光照贴图的 UV，因为它缺乏 UDIM（纹素大小）的分辨率。

烘焙期间不会计算该对象的光照贴图。

这种情况主要发生在具有较大表面积的大型网格中。

解决这个问题最有效的解决方案是将大网格分割成单独的部分。

然而，有时分裂并不实际——主要是在树的情况下。

因此，为了让引擎将整个布局展开为光照贴图，需要增加光照贴图布局的分辨率：然后，单击“应用更改”，我们将得到以下结果： 对于具有足够大且密集的 UV 布局的对象（如树），构建光照贴图可能会花费大量时间。

本质上，光照贴图的最小分辨率是使光照贴图适合 UDIM 所需的分辨率。

该值越高，对象的光照贴图的最小分辨率就越密集。

这意味着场景中默认的网格密度会更高，即使对于网格密度较低的对象也是如此。

光照贴图的密度越大，光/阴影烘焙的质量就越高。

因此，这会导致计算（烘焙）时间更长。

现在，让我们考虑一下我们公司的一个内部项目的示例。

要检查 UE 中光照贴图的密度，有一个名为 Lightmap Density 的内置可视化工具：要激活此模式，您还可以使用热键：Alt + 0。

在此示例中，我们可以看到各个网格的光照贴图有不同的颜色： 这些颜色表示： - 蓝色 - 光照贴图密度不足 蓝色 - 光照贴图密度不足 - 绿色 - 最佳光照贴图密度 绿色 - 最佳光照贴图密度 - 红色 - 光照贴图密度过高 红色 - 光照贴图密度过高 - 黑色 - 缺少光照贴图。

它也可能是由纳米网格引起的。

在光照烘焙中，Nanites 必须关闭黑色 - 没有光照贴图。

它也可能是由纳米网格引起的。

在光烘焙中，必须关闭 Nanites - 淡绿色 - 可移动网格 - 非静态 淡绿色 - 可移动网格 - 非静态 - 粉色 - 选定的对象 粉色 - 选定的对象 还有中间颜色，例如，蓝色和绿色之间 - 青色，以及绿色和红色之间 - 黄色。

粉色代表选定的网格。

正如前面提到的，光照贴图越密集，烘焙光的质量就越高，但烘焙过程所需的时间也就越长。

此外，还可以通过检查引擎中方格的大小来观察光照贴图密度：如示例所示，网格越密集，其上可视化的方格图案就越密集。

如果我们需要在引擎中使两个网格体达到大致相同的纹素密度（例如，如果一个网格体被分为 2 或 3 个部分，但它们最终具有不同的区域），那么在相同的光照贴图分辨率下，它们的密度将由于对象的不同区域而不同。

为了将所有内容对齐到一个共同的平均值，我们调整光照贴图参数并将方格图案的大小调整为大致相同。

由于相邻（分开的）网格上的光照贴图密度不同，烘焙的质量会有所不同 - 这可能特别明显，尤其是在大型 LED 屏幕或特写相机视图上。

光照贴图的密度可在所选网格体的“详细信息”面板下的场景中进行调整：要更改特定场景中网格体的光照贴图密度，您需要通过启用“覆盖”复选框来调整分辨率。

UE 支持的光照贴图的最大分辨率为 4096。
