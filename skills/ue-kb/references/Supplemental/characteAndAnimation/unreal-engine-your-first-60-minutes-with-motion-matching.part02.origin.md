# 运动匹配的前 60 分钟（续 2）

## 来源与状态

- 原始 URL：https://dev.epicgames.com/community/learning/tutorials/lwlG/unreal-engine-your-first-60-minutes-with-motion-matching
- 原始文件：unreal-engine-your-first-60-minutes-with-motion-matching.origin.md
- 分段：第 2/3 段

## 中文整理

### 数据库

接下来，创建第一个姿势搜索数据库。

以您刚刚创建的架构为目标，并将资产命名为 PSD_Walks。

该数据库将仅包含您的步行周期和等效的枢轴动画。

我们将为慢跑创建另一个数据库，并为怠速+停止和启动创建单独的数据库。

像这样分解数据库有助于指导运动匹配系统在处理稀疏数据集时选择正确的动画帧。

在内容浏览器中，选择 MM_Rifle_Walk_Fwd 以及 Bwd、Left、Right 和等效 Pivot 行走的变体。

这总共应该有八个序列。

将它们放入数据库的资产列表中。

数据库应根据模式中列出的通道自动分析序列。

如果您在资源列表中选择剪辑，它们应该在数据库视口中播放。

现在在数据库中进行以下更改： - 将非枢轴动画设置为循环。

为此，您可能需要在动画编辑器中打开动画序列（双击数据库中的资源将打开动画序列）。

将非枢轴动画设置为循环。

为此，您可能需要在动画编辑器中打开动画序列（双击数据库中的资源将打开动画序列）。

- 禁用非枢轴动画上的“姿势重新选择”标志 禁用非枢轴动画上的“姿势重新选择”标志 - 将循环成本偏差设置为 -0.01。

这降低了运动匹配系统从循环动画中选择帧的成本。

换句话说，它使得选择枢轴动画的可能性较小。

将循环成本偏差设置为 -0.01。

这降低了运动匹配系统从循环动画中选择帧的成本。

换句话说，它使得选择枢轴动画的可能性较小。

您的 PSD_Walks 数据库现在应如下所示： 现在，创建第二个数据库，并将其命名为 PSD_Jogs。

然后，对等效的点动和点动枢轴动画重复上述过程。

确保再次设置姿势标志的循环和重新选择。

您的数据库应如下所示： 现在创建第三个数据库，将其命名为 PSD_IdleAndStops。

将“空闲”动画和所有“停止”变体放入数据库中。

设置与之前相同的标志和属性，使数据库如下所示： 创建最终数据库并将其命名为 PSD_Starts。

放入所有开始变体动画。

再次设置标志和属性，使数据库如下所示：

### 正常化

最后，我们需要创建一个姿势搜索标准化资产。该资产应该在您希望动态切换活动数据库的情况下使用，正如我们稍后将设置的那样。创建规范化资产后，将其命名为 PSN_Locomotion 并将所有现有数据库添加到其中。注意：您还需要打开每个数据库并为其设置标准化资产。

### 设置您的动画蓝图

现在您已经设置了运动匹配资源，是时候在动画蓝图中使用它们了。创建一个新的动画蓝图并将其命名为 ABP_Mannequin。将目标骨架设置为我们从 Lyra Starter Project (SK_Mannequin) 迁移的人体模型骨架。返回到角色蓝图 (BP_ThirdPersonCharacter) 并在骨架网格体组件上设置新的动画蓝图。

### 运动匹配节点

返回动画蓝图，转到动画图表并放入运动匹配节点。在数据库输入引脚上，指定 PSD_IdleAndStops 作为数据库。运动匹配节点是任何运动匹配实现的核心。该节点将执行分析以确定动画数据的哪一帧与轨迹输入最匹配。

### 姿势历史节点

接下来，我们需要添加一个姿势历史节点。

### 生成轨迹数据

现在我们需要生成一些可以馈送到姿势历史节点的轨迹数据。

将变量添加到 Pose Search Query Trajectory 类型的动画蓝图中，并将其命名为 Trajectory： 现在，创建一个名为GenerateTrajectory 的新函数。

我们将用它来收集轨迹数据。

放入“姿势搜索生成轨迹”节点，然后按如下方式连接它： - 将轨迹变量连接到输入输出轨迹 将轨迹变量连接到输入输出轨迹 - 从输入轨迹数据引脚创建变量并重命名为轨迹生成数据 从输入轨迹数据引脚创建变量并将其重命名为轨迹生成数据 - 将输入增量时间引脚连接到获取增量秒函数 将输入增量时间引脚连接到获取增量Seconds 函数 - 将 In Anim Instance 连接到 Self 节点 将 In Anim Instance 连接到 Self 节点 - 从 In Out Desired Controller Yaw Last Update 引脚创建一个变量，并将其命名为 PreviousDesiredControllerYaw 从 In Out Desired Controller Yaw Last Update 引脚创建一个变量，并将其命名为 PreviousDesiredControllerYaw - 连接函数的其余部分并设置 Trajectory 变量 连接函数的其余部分并设置 Trajectory 变量 您的GenerateTrajectory 函数现在应该如下所示像这样：您可以避免通过姿势搜索生成轨迹节点生成轨迹，而是通过“生成轨迹”属性直接在姿势历史节点上生成轨迹。

目前这是一个实验性功能。

现在，在动画蓝图的事件图表中，将生成轨迹函数连接到事件蓝图更新动画。

返回动画图表，在“姿势历史记录”节点上设置“轨迹”引脚以使用“轨迹”变量。

### 选择数据库

现在，我们将设置一些逻辑，根据玩家角色的移动，根据上下文切换传递到运动匹配节点的活动数据库。但首先，让 PIE 看看我们当前的设置处于什么位置。您应该看到 Lyra 网格正在播放空闲动画。运动匹配节点已从 PSD_IdleAndStops 数据库中的数据中选择动画。当我们闲置时这很棒，但是一旦我们移动角色，很明显我们需要将数据库交换到那些包含...

### 选择器表

### 收集一些状态数据

### 完成我们的选择器设置

### 设置数据库

### 中断模式

### 使用动画变形修复覆盖范围

### 方向扭曲

### 步幅扭曲

### 细化动画选择

### 通过倒带调试器识别问题
