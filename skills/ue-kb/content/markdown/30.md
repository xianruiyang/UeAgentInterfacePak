# 材料中最重要的30个节点

# 材料中最重要的30个节点

## 来源与状态

- 原始 URL：https://dev.epicgames.com/community/learning/tutorials/DPax/unreal-engine-fortnite-30-most-important-nodes-in-materials

## 运行时分类

- 类型：图文教程
- 判断：运行时未识别到核心视频信号，可整理正文约 5216 字符。

## 摘要

我根据文档编制了一个包含 30 个最常用节点及其简短描述的表格。然而，我最初是在我的...

## 中文整理

### 概览

＃|名称 |描述 |输入|输出 |应用1 |纹理样本|用于处理纹理的核心节点。

|紫外线，| RGBA |纹理操纵。

2 |乘|将两个值相乘。

|甲、乙|结果 |亮度控制，纹理混合。

3 |添加|添加两个值。

|甲、乙|结果 |减轻纹理。

4 |莱普|根据 alpha 在两个值之间进行插值。

| A、B、阿尔法|结果 |渐变、动画。

5 |恒定|创建固定值。

|无 |数量 |参数调整。

6 |常数 3 向量 |创建固定的颜色值。

|无 |颜色（RGB）|基色设置。

7 |菲涅尔|计算菲涅尔效应。

|指数、基反射分数|效果强度|突出边缘，未来材料。

8 |平移器 |对 UV 坐标进行动画处理。

|坐标、速度|新紫外线|纹理动画。

9 |时间 |返回当前时间（以秒为单位）。

|无 |时间 |动画材料。

10 | 10世界地位|检索像素的世界坐标。

|无 |矢量 (XYZ) |全局效果，阴影。

11 | 11相机矢量|提供相机的方向。

|无 |矢量|与相机相关的效果。

12 | 12反射矢量|计算光的反射方向。

|无 |反射矢量|模拟反射。

13 |如果 |执行条件操作。

| A、B、阈值|选定值 |材料中的逻辑控制。

14 | 14从高度图法线 |从高度图纹理生成法线。

|高度图|法线|表面细节增强。

15 | 15正弦|计算输入值的正弦。

|输入|价值|脉动效果、动画。

16 | 16余弦|计算输入值的余弦。

|输入|价值|有节奏的动画。

17 | 17电源|将值提高到指定的幂。

|底数、指数 |结果 |对比度控制。

18 | 18一减 |从 1 中减去输入值。

|输入|结果 |反转纹理/值。

19 | 19绝对值（绝对值）|返回输入的绝对值。

|输入|正数 |使用负值。

20 |点积|计算两个向量的点积。

|甲、乙|数量 |照明、法线。

21 | 21叉积|计算两个向量的叉积。

|甲、乙|矢量|几何效应。

22 | 22附加向量|将两个值组合成一个向量。

|甲、乙|矢量|紫外线的形成。

23 | 23组件掩模|从向量中提取特定组件。

|输入，掩码|选定的组件|操纵颜色或矢量。

24 |紫外线坐标|生成标准 UV 坐标。

|无 |紫外线|使用纹理。

25 | 25旋转器|旋转 UV 坐标。

|坐标、速度|新紫外线|创建旋转纹理。

2...

用于处理纹理的纹理示例核心节点。

纹理操纵。

乘法 将两个值相乘。

亮度控制，纹理混合。

添加两个值。

减轻纹理。

根据 alpha 在两个值之间进行插值。

A、B、Alpha 渐变、动画。

常量 创建固定值。

参数调整。

Constant3Vector 创建固定颜色值。

颜色 (RGB) 基色设置。

计算菲涅尔效应。

指数、基础反射分数效果强度突出边缘、未来材料。

对 UV 坐标进行动画处理。

坐标、速度纹理动画。

返回当前时间（以秒为单位）。

动画材料。

世界位置 检索像素的世界坐标。

矢量 (XYZ) 全局效果，阴影。

相机矢量 提供相机的方向。

与相机相关的效果。

反射矢量 计算光的反射方向。

模拟反射。

执行条件操作。

A、B、阈值 选定值 材料中的逻辑控制。

从高度图法线 从高度图纹理生成法线。

高度图表面细节增强。

计算输入值的正弦。

脉动效果、动画。

计算输入值的余弦。

有节奏的动画。

将值提高到指定的幂。

底数、指数对比度控制。

一减 将输入值从 1 中减去。

反转纹理/值。

Abs（绝对） 返回输入的绝对值。

正数 使用负值。

点积 计算两个向量的点积。

照明、法线。

叉积 计算两个向量的叉积。

几何效应。

追加向量 将两个值组合成一个向量。

紫外线的形成。

组件掩码 从向量中提取特定组件。

输入、遮罩所选组件 操纵颜色或矢量。

UV 坐标 生成标准 UV 坐标。

使用纹理。

旋转 UV 坐标。

创建旋转纹理。

标量参数 用于更改材料值的参数。

自定义可调浮点参数 矢量参数 用于更改颜色或矢量的参数。

自定义可调节“矢量”参数 用户定义的 HLSL 代码。

取决于实施 复杂的影响。

深度淡入淡出 根据距离淡出纹理。

不透明度、淡入淡出距离透明材质。

场景颜色 检索场景颜色。

后期处理，效果。

如果此格式对任何人有帮助，请告诉我，我将继续填写该表。

这不是很容易，因为在这里使用表不是很方便。如果您需要更详细的节点描述，我也可以提供。

- 材料

## 相关链接

- 未识别到明确相关链接。


