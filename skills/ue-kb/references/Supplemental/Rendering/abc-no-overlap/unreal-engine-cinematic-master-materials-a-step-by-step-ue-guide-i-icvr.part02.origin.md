# 电影主材料：UE 分步指南 I ICVR（续 2）

## 来源与状态

- 原始 URL：https://dev.epicgames.com/community/learning/tutorials/7d0y/unreal-engine-cinematic-master-materials-a-step-by-step-ue-guide-i-icvr
- 原始文件：unreal-engine-cinematic-master-materials-a-step-by-step-ue-guide-i-icvr.origin.md
- 分段：第 2/3 段

此方案中的金属值取自类别 01 - 纹理贴图中定义的单独金属贴图（默认为黑色）。

ORD 到 ORMD 将 AO/粗糙度/位移压缩纹理转换为 AO/粗糙度/金属/位移格式。

此方案中的金属值取自类别 01 - 纹理贴图中定义的单独金属贴图（默认为黑色）。

- RMA 到 ORM 将粗糙度/金属/AO 打包纹理转换为 AO/粗糙度/金属/位移格式。

置换值源自类别 01 - 纹理贴图中定义的单独置换贴图。

RMA 到 ORM 将粗糙度/金属/AO 打包纹理转换为 AO/粗糙度/金属/位移格式。

置换值源自类别 01 - 纹理贴图中定义的单独置换贴图。

- 高光贴图 允许使用高光贴图。

如果禁用，则会应用类别 02 - 材质调整中的镜面反射值。

高光贴图 启用高光贴图的使用。

如果禁用，则会应用类别 02 - 材质调整中的镜面反射值。

- 分离的 ORM 激活 ORMD 管道。

关闭时，单独的纹理贴图用于 AO、粗糙度、金属和位移值。

分离的 ORM 激活 ORMD 管道。

关闭时，单独的纹理贴图用于 AO、粗糙度、金属和位移值。

- 粗糙度贴图允许使用粗糙度纹理，无论是包含在 ORMD 贴图中还是作为独立纹理。

如果禁用，则使用类别 02 - 材质调整中的粗糙度/金属值的 G 通道。

粗糙度贴图 允许使用粗糙度纹理，无论是包含在 ORMD 贴图中还是作为独立纹理。

如果禁用，则使用类别 02 - 材质调整中的粗糙度/金属值的 G 通道。

### 纹理贴图

该块定义纹理到材质中相应槽的分配。 - AO 贴图：线性、DXT1 (RGB) AO 贴图：线性、DXT1 (RGB) - 基色贴图：sRGB、DXT5 (RGB+A) 基色贴图：sRGB、DXT5 (RGB+A) - 发射贴图：sRGB、DXT1 (RGB) 发射贴图：sRGB、DXT1 (RGB) - 金属贴图：线性、DXT1 (RGB) 金属贴图：线性、DXT1 (RGB) - 正常贴图：线性，BC5 (RG) 法线贴图：线性，BC5 (RG) - ORM 贴图：线性，DXT5 (RGBA) ORM 贴图：线性，DXT5 (RGBA) - 高光贴图：线性，DXT1 (RGB) 高光贴图：线性，DXT1 (RGB) 此外，此类别还包括一组与上面列出的名称相同但以 VT 结尾的纹理贴图。当类别 03 - 材质覆盖中启用“使用虚拟纹理采样器”选项时，默认情况下会使用这些选项。

### 材质调整

该块用于调整核心材质参数 - 基色控制 用于修改基色图的参数： R = 饱和度：调整颜色强度。

G = 亮度：控制整体亮度或暗度。

B = 对比度：修改明暗区域之间的差异。

基色控制用于修改基色图的参数： - R = 饱和度：调整颜色强度。

R = 饱和度：调整颜色强度。

- G = 亮度：控制整体亮度或暗度。

G = 亮度：控制整体亮度或暗度。

- B = 对比度：修改明暗区域之间的差异。

B = 对比度：修改明暗区域之间的差异。

- 基色色调 调整基色图的颜色和亮度。

基色色调 调整基色图的颜色和亮度。

- AO / 粗糙度控制 允许修改 AO 和粗糙度图值： R = AO 最小值 G = AO 最大值 B = 粗糙度最小值 A = 粗糙度最大值 AO / 粗糙度控制 允许修改 AO 和粗糙度图值： - R = AO 最小值 R = AO 最小值 - G = AO 最大值 G = AO 最大值 - B = 粗糙度最小值 B = 粗糙度最小值 - A = 粗糙度最大值 A = 粗糙度最大值 - 这些最小/最大对可实现多种操作：通过设置最小 = 最大来调整整体亮度。

通过仅修改一个参数来限制范围。

通过设置 Max = 0 和 Min = 1 反转值。

使用线性斜坡改变暗区和亮区之间的对比度和平衡。

亮度保持在 0-1 的范围内。

这些最小/最大对可实现多种操作： - 通过设置最小 = 最大来调整整体亮度。

通过设置 Min = Max 来调整整体亮度。

- 通过仅修改一个参数来限制范围。

通过仅修改一个参数来限制范围。

- 通过设置 Max = 0 和 Min = 1 反转值。

通过设置 Max = 0 和 Min = 1 反转值。

- 使用线性斜坡改变暗区和亮区之间的对比度和平衡。

使用线性斜坡改变暗区和亮区之间的对比度和平衡。

- 亮度保持在 0-1 范围内。

亮度保持在 0-1 的范围内。

- 发射颜色 定义不使用发射贴图时的发光颜色。

发射颜色 定义不使用发射贴图时的发光颜色。

- 发射颜色乘数 用于控制发射颜色亮度的乘数。

发射颜色乘数 用于控制发射颜色亮度的乘数。

- 金属控制（最小/最大） 在定义的范围内调整金属贴图值。

金属控制（最小/最大） 在定义的范围内调整金属贴图值。

- 法线贴图强度 控制表面细节法线贴图的强度。

法线贴图强度 控制表面细节法线贴图的强度。

- 镜面反射值 设置未应用镜面反射贴图时的镜面反射强度。

镜面反射值 设置未应用镜面反射贴图时的镜面反射强度。

- UV 控制 用于修改 UV 映射的参数： R = UV 比率 X：相对于垂直轴的水平轴缩放。

G = UV 比率 Y：垂直轴相对于水平方向的缩放比例。

B = UV 平铺因子：控制 UV 比例和纹理平铺，包括三平面投影。

UV 控制用于修改 UV 映射的参数： - R = UV 比率 X：相对于垂直轴的水平轴缩放...

### 材质覆盖

### 瓷砖破碎机

### 分层细节纹理

### 参数

### 纹理细节

### PDO 接触阴影

### 次表面散射 (SSS)

### 纳米位移

### 各向异性

### 运行时虚拟纹理

### 叠加子材质

### 路径追踪

### 光线追踪

### 材质层

## 相关链接
